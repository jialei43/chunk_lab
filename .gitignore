# 本机配置：数据目录等因机器而异的设置，不纳入版本管理
labconfig.json

# 数据目录已统一移到仓库之外（默认 ~/MinerU/chunk_lab），仓库内只保留代码。
# 以下规则用于兼容把数据目录配置回仓库内的情况，正常不会命中。
corpus/
runs/
reports/
baselines/
mineru_out/
uploads/

# Python 运行期产物
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器与系统文件
.DS_Store
.idea/
.vscode/
